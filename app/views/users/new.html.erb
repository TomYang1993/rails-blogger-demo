<div class="w-full">
  <h2 class="text-4xl font-extrabold dark:text-white">Create New User</h2>

  <% if @user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize( @user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <%  @user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render "form"  %>

</div>